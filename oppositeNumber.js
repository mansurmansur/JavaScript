/**
 * returns opposite number. this function takes in a number and
 * if negative returns positve and if positive returns negative same number.
 */

 function oppositeNumber(num){
     return -num;
 }


 //invoke the function
 console.log( oppositeNumber(15));
